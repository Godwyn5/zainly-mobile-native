// MOVED — this file is not picked up by Jest: the project's jest config
// excludes <rootDir>/app/ via testPathIgnorePatterns (verified: `npx jest
// app/onboarding-v2/__tests__/learningModeSelection.test.tsx` reports
// "No tests found"). The real, active test now lives at
// src/lib/__tests__/learningModeSelection.test.tsx (same convention as the
// pre-existing src/lib/__tests__/todayScreenNoPlan.test.tsx, which also
// imports a screen from app/ into a test file placed under src/).
//
// This stub is intentionally left empty (no test bodies) to avoid a
// duplicate/dead copy of the real test logic. It could not be deleted
// (no file-delete capability available in this session) — please delete
// this file manually: app/onboarding-v2/__tests__/learningModeSelection.test.tsx
