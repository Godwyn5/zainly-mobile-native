// ─── MOVED ──────────────────────────────────────────────────────────────────
// This test file was moved to src/lib/__tests__/todayScreenNoPlan.test.tsx
// because jest's testPathIgnorePatterns (package.json) excludes <rootDir>/app/
// from test discovery — a test file placed here would silently never run.
// This file is intentionally left empty (no tests) to avoid confusion.
export {};
